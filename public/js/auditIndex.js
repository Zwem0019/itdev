const pinButtons = document.querySelectorAll("#pin");
for (let i = 0; i < pinButtons.length; i++) {
    pinButtons[i].addEventListener("click", function () {
        if (pinButtons[i].value === 1) {

        };
    });
}
